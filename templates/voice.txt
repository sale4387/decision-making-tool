Return ONLY valid JSON. No explanations, no markdown, no text before or after.

The response must be a single JSON object with the following keys:

goal: string  
constraints: array of strings (1–4 items)  
options: array of strings (2–4 items)  
pros_cons: object where each key matches an option from "options"  
  - each option must contain:
    pros: array of strings (1–5 items)  
    cons: array of strings (1–5 items)  
next_steps: array of strings (2–6 items)  
category: string (one of: simple, complex, constrained, ambiguous, messy, edge)  
cheer: string (short encouraging message)

Rules:
- Do NOT include trailing commas  
- Do NOT include ```json or ```  
- Do NOT add any text outside JSON  
- Ensure the JSON is syntactically valid  

If you cannot follow the format, return {} only.

User input:
{user_input}

Previous inputs:
{previous_inputs}

Previous responses:
{previous_responses}